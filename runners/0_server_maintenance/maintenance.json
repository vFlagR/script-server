{
  "name": "#0 Server Maintenance",
  "script_path": "maintenance.sh",
  "working_directory": "./conf/scripts/0_server_maintenance/",
  "parameters": [
    {
      "name": "Command",
      "required": true,
      "type": "list",
      "default": "Update",
      "description": "A list of commands that can be run on the host",
      "values": [
        "Update",
        "Reboot"
      ]
    }
  ]
}
