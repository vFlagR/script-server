{
  "name": "#3 Docker",
  "script_path": "docker.sh",
  "working_directory": "./conf/scripts/3_docker/",
  "parameters": [
    {
      "name": "Command",
      "required": true,
      "type": "list",
      "default": "Monitor",
      "description": "A list of commands for maintaining/monitoring docker",
      "values": [
        "Monitor",
        "Restart"
      ]
    }
  ]
}
