{
  "name": "#3 Docker",
  "script_path": "scripts/docker.sh",
  "working_directory": "./conf",
  "parameters": [
    {
      "name": "Command",
      "required": true,
      "type": "list",
      "default": "Monitor",
      "description": "A list of commands for maintaining/monitoring docker",
      "values": [
        "Monitor",
        "Restart"
      ]
    }
  ]
}