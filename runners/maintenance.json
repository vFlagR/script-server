{
  "name": "#0 Server Maintenance",
  "script_path": "scripts/maintenance.sh",
  "working_directory": "./conf",
  "parameters": [
    {
      "name": "Command",
      "required": true,
      "type": "list",
      "default": "Update",
      "description": "A list of commands that can be run on the host",
      "values": [
        "Update",
        "Reboot"
      ]
    }
  ]
}